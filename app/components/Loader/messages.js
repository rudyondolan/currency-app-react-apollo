/*
 * Loader Messages
 *
 * This contains all the text for the Loader component.
 */

import { defineMessages } from 'react-intl';

export default defineMessages({
  loading: {
    id: 'app.components.Loader.loading',
    defaultMessage: 'Loading...',
  },
});
